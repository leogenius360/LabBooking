<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for network access, essential for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".LabBookingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabBooking">

        <!-- MainActivity - This is your app's entry point, so it must be exported. -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities - Explicitly set exported="false" as they are internal to your app
             and don't have intent filters for external access. This is a security best practice. -->
        <!--        <activity-->
        <!--            android:name=".activities.LoginActivity"-->
        <!--            android:exported="false" />-->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".activities.BookingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AllBookingsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ManageLabsActivity"
            android:exported="false" />
        <!--        <activity-->
        <!--            android:name=".activities.ManageUsersActivity"-->
        <!--            android:exported="false" />-->

        <!-- Firebase Cloud Messaging Service (FCM) -->
        <service
            android:name=".services.BookingNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
